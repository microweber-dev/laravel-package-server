on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          chmod +rwx build-packages.sh
          chmod +rwx build-packages-plesk.sh
          
